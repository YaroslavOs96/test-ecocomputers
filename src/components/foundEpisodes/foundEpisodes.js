import React, { Component } from "react";

import './foundEpisodes.css'

export default class foundEpisodes extends Component {

        render() {
        return (
            <div>
                Эпизод такой-то
            </div>
        )
    }
}